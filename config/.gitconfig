[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[pull]
	ff = only
	default = current
[push]
	default = current
[color]
	ui = auto
[fetch]
	prune = true
[core]
	autocrlf = false
	quotepath = false
